[build]
builder = "NIXPACKS"

[[services]]
name = "web"
startCommand = "uvicorn webhook_server:app --host 0.0.0.0 --port $PORT"

# Organizer agents run via LOCAL cron on the Mac (crontab -l), not Railway:
# Railway is paid usage and the file organizer must scan the local filesystem.
